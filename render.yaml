services:
  - type: worker
    name: vahanx-rc-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python vahanx_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
